Paragraph,Ground truth tasks,Program tasks,Partial Ratio
"Request the test.php page, but ignore the return results.",request page,ignore return results,50
"Request the test.php page, but ignore the return results.",ignore return results,ignore return results,100
Request the test.php page and send some additional data along (while still ignoring the return results).,request page,request test.php page,75
Request the test.php page and send some additional data along (while still ignoring the return results).,send additional data,request test.php page,25
Request the test.php page and send some additional data along (while still ignoring the return results).,ignore return results,request test.php page,38
Get another page on the same domain. Outputs to console both the data returned and the type of data returned.,get another page on the same domain,get page on same domain,83
"The Promise interface also allows jQuery's Ajax methods, including $.get(), to chain multiple .done(), .fail(), and .always() callbacks on a single request, and even to assign these callbacks after the request may have completed. If the request is already complete, the callback is fired immediately.",chain multiple callbacks on a single request
Pass arrays of data to the server (while still ignoring the return results).,pass arrays of data to the server
Pass arrays of data to the server (while still ignoring the return results).,ignore return results
